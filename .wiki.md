# Project Summary

AtomForge 是面向产品与开发团队的 AI 应用原型工作台。用户输入产品需求后，系统通过 Planner、Designer、Engineer、Reviewer 四阶段协作生成产品方案、界面代码与可预览结果，并支持审批、二次修改、版本历史、回滚和受限预览。

项目采用 Vite + React 前端与 Python 后端，AI 能力由 Atoms AIHub `client.ai.gentxt` 驱动。工作区默认使用浏览器本地存储；登录后可将工作区和版本同步到云端，按账号隔离历史记录，并通过会话锁避免同一账号并发编辑冲突。

# Project Module Description

- **工作台**：承载需求输入、对话、Agent 时间线、方案卡片、代码结果、预览和版本历史。
- **AI 编排**：按 Planner → Designer → Engineer → Reviewer 执行生成流程，支持重试、阶段状态和二次修改。
- **项目状态管理**：使用 reducer、selectors 和 Hook 管理项目、阶段、结果及当前版本。
- **本地持久化**：通过 localStorage 保存单工作区数据，兼容 `atomforge.workspace.v1`，当前键名为 `atomforge.workspace.v2`。
- **云端同步**：登录后同步工作区与版本，按账号隔离数据；未登录或同步失败时保留本地存储作为降级方案。
- **版本控制**：保存生成版本，支持切换历史版本和回滚后继续编辑。
- **会话锁**：同一账号同一时间仅允许一个设备编辑；冲突设备进入只读状态，可查看历史并接管编辑。
- **锁生命周期**：支持心跳、过期检测和退出时释放锁，锁失效后停止写入原账号数据。
- **同步状态反馈**：顶部显示本地存储、同步中、已同步云端和同步失败等状态。
- **预览安全**：校验、加固生成 HTML，并通过受限 iframe 展示。
- **Atoms AIHub**：登录后调用真实 AI，未登录或模型不可用时使用 Demo 生成器。
- **认证与用户**：提供登录回调、用户信息、受保护路由及管理员路由支持。
- **后端服务**：提供 AIHub、认证、用户、设置、存储、云端工作区、版本、会话锁、健康检查及支付接口。
- **博客内容**：支持博客索引、文章渲染、路由生成和站点地图生成。
- **质量保障**：覆盖 AI 解析、工作流、状态 reducer、预览安全、云端同步、会话锁和工作台交互测试。

# Directory Tree

```text
.
├── README.md                         项目说明、源码仓库与在线体验入口
└── app
    ├── .mgx/config.yaml              项目平台配置
    ├── start_app_v2.sh               应用启动脚本
    ├── backend
    │   ├── main.py                   后端应用入口
    │   ├── lambda_handler.py         Lambda 部署入口
    │   ├── requirements.txt          Python 依赖
    │   ├── requirements.default      默认依赖模板
    │   ├── alembic.ini               数据库迁移配置
    │   ├── alembic/                  迁移环境与脚本模板
    │   │   └── versions/
    │   │       └── 65283b70e82d_auto_update.py
    │   │                                  Forge 表结构迁移
    │   ├── core/                     配置、认证、数据库、加密与遥测
    │   ├── dependencies/             请求依赖注入
    │   ├── middlewares/              中间件
    │   ├── models/                   ORM 数据模型
    │   │   ├── forge_workspaces.py   云端工作区模型
    │   │   ├── forge_versions.py     云端版本模型
    │   │   └── forge_session_locks.py 会话锁模型
    │   ├── data_models/              Forge 数据模型定义
    │   │   ├── forge_workspaces.json 工作区结构
    │   │   ├── forge_versions.json   版本结构
    │   │   └── forge_session_locks.json 会话锁结构
    │   ├── routers/                  HTTP API 路由
    │   │   ├── forge_sync.py         云端同步接口
    │   │   ├── forge_workspaces.py   工作区接口
    │   │   ├── forge_versions.py     版本接口
    │   │   └── forge_session_locks.py 会话锁接口
    │   ├── schemas/                  请求与响应结构
    │   ├── services/                 业务服务
    │   │   ├── forge_sync.py         同步编排服务
    │   │   ├── forge_workspaces.py   工作区服务
    │   │   ├── forge_versions.py     版本服务
    │   │   └── forge_session_locks.py 会话锁服务
    │   ├── skills_docs/              AIHub、自定义 API、对象存储与 Web SDK 文档
    │   └── utils/                    日志等通用工具
    └── frontend
        ├── package.json              前端依赖与脚本
        ├── vite.config.ts            Vite 配置
        ├── vitest.config.ts          Vitest 测试配置
        ├── tailwind.config.ts        Tailwind 配置
        ├── tsconfig*.json            TypeScript 配置
        ├── index.html                前端 HTML 入口
        ├── public/                   favicon、robots 等静态资源
        ├── prerender/                博客预渲染与站点地图脚本
        └── src/
            ├── App.tsx               应用路由与页面容器
            ├── pages/                首页、认证回调、博客页面
            ├── components/            工作台、博客及通用 UI 组件
            ├── contexts/             认证上下文
            ├── hooks/                移动端、提示及工作区 Hook
            │   └── use-workspace.ts 工作区同步与锁接入
            ├── lib/                  API、认证、AI、项目状态与预览工具
            │   └── projects/
            │       └── cloud-sync.ts  云端同步与会话锁客户端
            ├── api/                  设置接口封装
            └── *.css                 全局与应用样式
```

# File Description Inventory

## 根目录

| 路径 | 内容 |
|---|---|
| `README.md` | 项目说明、源码仓库和在线体验入口 |

## 后端

| 路径 | 内容 |
|---|---|
| `backend/main.py` | 创建并配置后端应用 |
| `backend/core/` | 认证、配置、数据库、枚举、环境、加密和请求/SQL 遥测 |
| `backend/models/forge_workspaces.py` | Forge 云端工作区 ORM 模型 |
| `backend/models/forge_versions.py` | Forge 云端版本 ORM 模型 |
| `backend/models/forge_session_locks.py` | Forge 会话锁 ORM 模型 |
| `backend/data_models/forge_workspaces.json` | 工作区数据模型定义 |
| `backend/data_models/forge_versions.json` | 版本数据模型定义 |
| `backend/data_models/forge_session_locks.json` | 会话锁数据模型定义 |
| `backend/routers/forge_sync.py` | 云端同步接口 |
| `backend/routers/forge_workspaces.py` | 工作区增删改查接口 |
| `backend/routers/forge_versions.py` | 版本读写与历史接口 |
| `backend/routers/forge_session_locks.py` | 会话获取、心跳、接管和释放接口 |
| `backend/services/forge_sync.py` | 工作区与版本同步编排 |
| `backend/services/forge_workspaces.py` | 云端工作区业务服务 |
| `backend/services/forge_versions.py` | 云端版本业务服务 |
| `backend/services/forge_session_locks.py` | 会话锁及冲突处理服务 |
| `backend/alembic/versions/65283b70e82d_auto_update.py` | Forge 相关表结构迁移 |
| `backend/routers/aihub.py` | AIHub 相关接口 |
| `backend/routers/auth.py` | 认证接口 |
| `backend/routers/health.py` | 健康检查接口 |
| `backend/routers/settings.py` | 设置接口 |
| `backend/routers/storage.py` | 存储接口 |
| `backend/routers/user.py` | 用户接口 |
| `backend/services/aihub.py` | AIHub 调用与业务封装 |
| `backend/services/auth.py` | 认证服务 |
| `backend/services/database.py` | 数据库服务 |
| `backend/services/storage.py` | 对象存储服务 |
| `backend/services/user.py` | 用户服务 |
| `backend/services/payment.py` | 支付服务 |
| `backend/schemas/` | AI、认证、存储及 Forge 数据结构 |
| `backend/skills_docs/` | 平台能力和接入规范 |

## 前端

| 路径 | 内容 |
|---|---|
| `frontend/src/components/workspace/workspace-shell.tsx` | 工作台外壳、同步状态和锁冲突界面 |
| `frontend/src/components/workspace/workspace-shell.test.tsx` | 工作台及锁状态交互测试 |
| `frontend/src/hooks/use-workspace.ts` | 工作区状态、云端同步和会话锁操作 |
| `frontend/src/lib/projects/cloud-sync.ts` | 云端工作区、版本和会话锁客户端 |
| `frontend/src/components/workspace/` | 工作台输入、对话、时间线、方案、预览、结果和版本组件 |
| `frontend/src/components/blog/` | 博客文章布局和 Markdown 渲染 |
| `frontend/src/components/ui/` | 基于 shadcn/ui 风格的通用组件库 |
| `frontend/src/lib/ai/` | AI 模型、提示词、解析、运行时、阶段、Demo 和工作流 |
| `frontend/src/lib/projects/` | 项目类型、本地存储、reducer、selectors、云同步及测试 |
| `frontend/src/lib/preview/` | HTML 校验、加固和预览安全测试 |
| `frontend/src/lib/api.ts` | 后端 API 客户端 |
| `frontend/src/lib/auth.ts` | 前端认证工具 |
| `frontend/src/contexts/AuthContext.tsx` | 全局认证状态 |
| `frontend/src/pages/` | 应用、认证回调和博客页面 |
| `frontend/src/App.tsx` | 应用入口及路由组合 |
| `frontend/src/App.css`、`index.css` | 应用主题和全局样式 |
| `frontend/prerender/` | 博客静态预渲染脚本 |

# Technology Stack

- **前端**：React、TypeScript、Vite
- **UI**：Tailwind CSS、shadcn/ui 风格组件
- **状态管理**：React Context、Reducer、localStorage
- **云端同步**：Forge 工作区、版本与会话锁 API
- **AI**：Atoms AIHub、`client.ai.gentxt`
- **后端**：Python、FastAPI 风格路由架构
- **数据层**：SQLAlchemy、Alembic
- **平台能力**：Atoms Cloud、认证、对象存储、支付、Lambda Handler
- **测试**：Vitest、React Testing Library、TypeScript 类型检查
- **代码质量**：ESLint、生产构建校验
- **内容系统**：Markdown 博客、预渲染和站点地图生成

# Usage

## 前端安装与开发

```bash
cd app/frontend
npm install
npm run dev
```

## 前端质量检查

```bash
cd app/frontend
npm run typecheck
npm run lint
npm run test
npm run build
```

## 后端安装与运行

```bash
cd app/backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```

也可以从项目根目录执行：

```bash
bash app/start_app_v2.sh
```

## 数据库迁移

```bash
cd app/backend
alembic upgrade head
```

## 配置要求

- 配置数据库、认证、Atoms Cloud、对象存储和支付相关环境变量。
- 前端 API 与站点配置位于 `src/lib/config.ts`、`site.config.json` 和 Vite 配置中。
- 登录后启用账号级云端同步与会话锁；未登录时使用本地存储和 Demo AI。
- 不要提交 token、上传文件、交接文档或其他私密配置。
- `uploads/`、`AGENTS.md` 等私密资料由 `.gitignore` 排除，不进入公开仓库。
