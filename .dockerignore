# Dependencies and build output are produced inside the image.
node_modules
.next
out
dist
coverage
*.tsbuildinfo

# Secrets never enter the build context; the container reads them at run time.
.env
.env.*
!.env.example

# Local noise.
.git
.gitignore
.DS_Store
*.log

# Private challenge material.
AGENTS.md
.workbuddy
docs/internal
